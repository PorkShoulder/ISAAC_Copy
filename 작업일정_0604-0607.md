# 작업 일정 (6/4 목 오후 ~ 6/7 일 오후 마감)

기준일: 2026-06-04 (목) / 마감: 2026-06-07 (일) 오후

---

## 완료된 작업

- 각 클래스 헤더 inline 배열 추가 (Obstacle / Item / Door / Npc) ✅
- 타일 BLOCK 충돌 기본 동작 (IsBlocked + MovementComponent 연결 + SetTileMap 등록) ✅
- 버그 수정: Actor `_level` 멤버 복구, RoomEditor static 정의 등 ✅

---

## 남은 작업 (우선순위 / 규모)

| # | 작업 | 규모 | 상태 |
|---|------|------|------|
| 1 | 타일 아웃라인 on/off (`_showTileCollider` 연결) | XS | 코드 제시됨, 적용+테스트만 |
| 2 | 오브젝트 충돌체 표시 (`_showObjectCollider` → `CollisionComponent::Render` 연결, `RoomEditor::_showCollider` 정리, 체크박스 2개) | S | 미적용 |
| 3 | GetMouseDown 클릭 한 번 처리 (엣지 트리거 검증/마무리) | S | Down/Hold/Up 분리됨, 검증 필요 |
| 4 | 스냅 (오브젝트 배치 그리드 스냅) | M | 미해결 |
| 5 | 오브젝트 Save/Load (Actor + 각 타입 + Level 연동) | L | 미착수 (가장 큼) |
| 6 | (선택) SetTexture 공통화 리팩터링 | S | 논의만 |
| 7 | (선택) 캐릭터 AABB 기준 타일 막기 (④) | S | 보류 |

---

## 일자별 계획

### 6/4 (목) 오후 — 작은 마무리
- [ ] #1 타일 아웃라인 on/off 연결 + 테스트
- [ ] #2 오브젝트 충돌체 표시: `CollisionComponent::Render`에 `_showObjectCollider` 검사, 기존 `RoomEditor::_showCollider` 제거, 체크박스 2개 추가
> 둘 다 코드가 거의 준비돼 있어 오늘 마무리 목표

### 6/5 (금) — 입력 + 스냅 착수
- [ ] #3 GetMouseDown 한 번 처리 마무리 (클릭 1회만 반응 검증)
- [ ] #4 스냅 작업 착수 (배치 좌표를 그리드에 정렬)

### 6/6 (토) — 스냅 완료 + Save/Load 착수
- [ ] #4 스냅 완료 + 테스트
- [ ] #5 오브젝트 Save/Load 착수
  - `Actor::Save/Load` 가상 함수 정의
  - 각 타입(Obstacle/Item/Door/Monster) override: 타입·텍스처·트랜스폼 저장
  - `Level::Save/Load`의 `it.second->Save(file)` 주석 해제 + 액터 수/타입 헤더 기록

### 6/7 (일) 오전~오후 — Save/Load 마무리 + 통합 테스트 [마감]
- [ ] #5 Save/Load Level 연동 완성 (Load 시 타입별 SpawnActor 후 복원)
- [ ] 전체 통합 테스트: 배치 → 저장 → 로드 → 충돌/표시 확인
- [ ] 여유 시 #6 / #7 정리
- [ ] **오후 마감**

---

## 리스크 / 메모

- #5 Save/Load가 가장 큰 변수. 토요일에 착수가 늦어지면 일요일 오후 마감이 빡빡해짐 → 토요일 오전부터 우선 착수 권장.
- Load 시 타입 식별을 위해 저장 포맷에 `eActorType`을 액터마다 먼저 기록해야 함.
- #1, #2는 의존성 없는 독립 작업이라 막히면 순서 바꿔도 무방.
